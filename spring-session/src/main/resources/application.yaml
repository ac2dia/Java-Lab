logging:
  level:
    com.example.springsession: debug

spring:
  session:
    store-type: redis
    redis:
      flush-mode: on_save
      namespace: spring:session
  redis:
    host: localhost
    password:
    port: 6379